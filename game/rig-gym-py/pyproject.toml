[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "rig-gym-py"
version = "0.1.0"
description = "Python bridge to the RIG deterministic gym (PyO3/maturin)"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
