[package]
name = "rig-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "RIG deterministic physics core (Rust → WASM)."

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
